require('dotenv').config();
const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'config', 'databaseConfig.js'),
    'migrations-path': path.resolve(__dirname, 'database', 'migrations')
}